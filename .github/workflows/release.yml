---
name: Build SSM Cloud Backend
on:
  push:
    branches:
      - "**"
    tags:
      - "*"
jobs:
  build-linux:
    runs-on: ubuntu-latest
    outputs:
      deploy_tag: ${{ steps.imagetag.outputs.deploy_tag }}
    steps:
      - uses: actions/checkout@v7
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: "go.mod"
      - name: Install dependencies
        run: |
          go mod edit -dropreplace=github.com/SatisfactoryServerManager/ssmcloud-resources || true
          go mod tidy
      - name: Build
        run: go build -o ./release/linux/ssmcloud-backend cmd/backend/main.go
      - run: if [ ! -f ./release/linux/ssmcloud-backend ]; then exit 1; fi
      - run: mkdir -p ./docker/release >/dev/null && cp ./release/linux/* ./docker/release/.
      - uses: vimtor/action-zip@v1.3
        with:
          files: release/linux/ssmcloud-backend
          recursive: false
          dest: SSMCloud-Backend-Linux-amd64.zip
      - uses: actions/upload-artifact@v7
        with:
          name: ssm-build-linux
          path: ${{ github.workspace }}/SSMCloud-Backend-Linux-amd64.zip
      - name: Get version
        id: versionnum
        run: echo "version=$(cat VERSION)" >> $GITHUB_OUTPUT
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Determine image tags
        id: imagetag
        run: |
          IMAGE=mrhid6/ssmcloud-backend
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            DEPLOY_TAG="v${{ steps.versionnum.outputs.version }}"
            echo "deploy_tag=$DEPLOY_TAG" >> "$GITHUB_OUTPUT"
            {
              echo "push_tags<<EOF"
              echo "$IMAGE:latest"
              echo "$IMAGE:$DEPLOY_TAG"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
          else
            DEPLOY_TAG="dev-${{ github.run_number }}"
            echo "deploy_tag=$DEPLOY_TAG" >> "$GITHUB_OUTPUT"
            {
              echo "push_tags<<EOF"
              echo "$IMAGE:dev"
              echo "$IMAGE:$DEPLOY_TAG"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
          fi
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: ./docker
          push: true
          tags: ${{ steps.imagetag.outputs.push_tags }}
  build-windows:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v7
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: "go.mod"
      - name: Install dependencies
        run: |
          go mod edit -dropreplace="github.com/SatisfactoryServerManager/ssmcloud-resources" || $true
          go mod tidy
      - name: Build
        run: go build -o ./release/windows/ssmcloud-backend.exe cmd/backend/main.go
      - uses: vimtor/action-zip@v1.3
        with:
          files: release/windows/ssmcloud-backend.exe
          recursive: false
          dest: SSMCloud-Backend-Windows-x64.zip
      - uses: actions/upload-artifact@v7
        with:
          name: ssm-build-win64
          path: ${{ github.workspace }}/SSMCloud-Backend-Windows-x64.zip
  release:
    if: startsWith(github.ref, 'refs/tags/')
    needs:
      - build-linux
      - build-windows
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Download a Linux artifact
        uses: actions/download-artifact@v8
        with:
          name: ssm-build-linux
      - name: Download a Win64 artifact
        uses: actions/download-artifact@v8
        with:
          name: ssm-build-win64
      - run: ls -l
      - name: Get version
        id: versionnum
        run: echo "version=$(cat VERSION)" >> $GITHUB_OUTPUT
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ steps.versionnum.outputs.version }}
          release_name: Release v${{ steps.versionnum.outputs.version }}
          prerelease: false
          draft: false
          files: |
            ./SSMCloud-Backend-Linux-amd64.zip
            ./SSMCloud-Backend-Windows-x64.zip
  deploy:
    needs:
      - build-linux
    runs-on: ubuntu-latest
    steps:
      - name: Install Octopus CLI
        uses: OctopusDeploy/install-octopus-cli-action@v4
        with:
          version: latest
      - name: Deploy Backend
        id: run_a_runbook_in_octopus_deploy
        env:
          OCTOPUS_API_KEY: ${{ secrets.OCTOPUSSERVERAPIKEY }}
          OCTOPUS_URL: ${{ secrets.OCTOPUSSERVERURL }}
          OCTOPUS_SPACE: ${{ secrets.OCTOPUSSERVER_SPACE }}
          ENVIRONMENT: ${{ startsWith(github.ref, 'refs/tags/') && 'Prod' || 'Dev' }}
          IMAGE_TAG: ${{ needs.build-linux.outputs.deploy_tag }}
        run: |
          octopus runbook run \
            --space "$OCTOPUS_SPACE" \
            --project 'ssmcloud' \
            --git-ref 'refs/heads/main' \
            --name 'Deploy SSM Backend' \
            --environment "$ENVIRONMENT" \
            --package-version "$IMAGE_TAG" \
            --package "*:$IMAGE_TAG" \
            --no-prompt
